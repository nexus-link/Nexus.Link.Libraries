namespace Nexus.Link.Libraries.Core.Platform.Services
{
    /// <summary>
    /// If an interface that implements this has a dependency injection, then controllers can be injected.
    /// </summary>
    public interface IControllerInjector
    {
    }
}